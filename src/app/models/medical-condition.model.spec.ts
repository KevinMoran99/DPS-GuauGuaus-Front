import { MedicalCondition } from './medical-condition.model';

describe('MedicalCondition', () => {
  it('should create an instance', () => {
    expect(new MedicalCondition()).toBeTruthy();
  });
});
